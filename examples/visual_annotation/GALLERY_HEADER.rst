Examples
========

Task-sized executable examples built on the public ``visual_annotation`` API.
Each page keeps the annotation call close to its visible result and separates
input setup from the behavior being demonstrated.

These examples are hermetic: they annotate local images and require no external
services or credentials. Required CI can import them without executing the live
workflow, while Sphinx-Gallery renders the real annotated output.
